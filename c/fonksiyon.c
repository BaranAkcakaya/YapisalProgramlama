#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

/*char *dondur(char *p,int deger){
	
	int deger1=strlen(p);
	
	if(deger>deger1){
		return NULL;
	}
	else{
		printf("%p\n",p+deger);
		return(p+deger);
	}
		
}

int main(int argc, char *argv[]) {
	
	char dizi[]="yazilim";
	char *p=dondur(dizi,2);
	printf("%p\n",p);
	printf("%s",p);
	
	return 0;
}*/
